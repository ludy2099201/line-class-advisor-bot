# ── Web Framework ─────────────────────────────────────────────
flask>=3.0.0
gunicorn>=22.0.0

# ── HTTP & 環境 ────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0

# ── OpenAI ───────────────────────────────────────────────
openai>=1.30.0

# ── Redis（Session 持久化，Railway Redis Plugin 使用）──────────────
# 若不使用 Redis，可移除此行；系統會自動退回 In-Memory 模式
redis>=5.0.0
