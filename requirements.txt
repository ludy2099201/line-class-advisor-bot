# ── Web Framework ─────────────────────────────────────────────
flask>=3.0.0
gunicorn>=22.0.0

# ── HTTP & 環境 ────────────────────────────────────────────
requests>=2.34.2
python-dotenv>=1.2.2

# ── Google Gemini ──────────────────────────────────────────────
google-genai>=1.0.0

# ── Redis（Session 持久化，Railway Redis Plugin 使用）──────────────
# 若不使用 Redis，可移除此行；系統會自動退回 In-Memory 模式
redis>=5.0.0

# ── 背景工作（webhook 快速確認與獨立 worker）──────────────────
rq>=2.0.0
